"use client";
import {useState} from "react";
export default function Page(){const [name,setName]=useState("Steve");return <main className="toolPage"><div className="wrap"><div className="crumb"><a href="/">MineTools</a> / Skin Grabber</div><div className="toolIntro"><h1>Minecraft Skin Grabber</h1><p>Prepare a Minecraft username lookup for skin and UUID tools.</p></div><div className="workspace"><section className="panel"><h2>Player lookup</h2><div className="field"><label>Minecraft username</label><input value={name} onChange={e=>setName(e.target.value)} maxLength={16}/></div><p className="muted">Username: <strong>{name||"—"}</strong></p></section><section className="panel"><h2>Skin preview</h2><div className="skinPlaceholder">Skin API connection will be enabled after the public API/cache layer is added.</div></section></div><section className="help"><h2>Why the lookup is staged</h2><p>The page and UX are ready, but MineTools will not fake skin data. The live lookup needs a reliable public player-profile endpoint plus caching and error handling.</p></section></div></main>}