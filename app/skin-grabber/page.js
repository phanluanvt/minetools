"use client";
import {useState} from "react";
export default function Page(){const [name,setName]=useState("Steve"),[status,setStatus]=useState("");function lookup(){const n=name.trim();if(!/^[A-Za-z0-9_]{3,16}$/.test(n)){setStatus("Enter a valid 3–16 character Minecraft username.");return}setStatus("Live Mojang profile lookup requires a server-side API/cache route. MineTools will not invent a UUID or skin URL for "+n+".")}return <main className="toolPage"><div className="wrap"><div className="crumb"><a href="/">MineTools</a> / Skin Grabber</div><div className="toolIntro"><h1>Minecraft Skin Grabber</h1><p>Validate a Minecraft username and prepare safe skin/profile lookup without fabricated player data.</p></div><div className="workspace"><section className="panel"><h2>Player lookup</h2><div className="field"><label>Minecraft username</label><input value={name} onChange={e=>setName(e.target.value)} maxLength={16}/></div><button className="copyBtn" onClick={lookup}>Check username</button>{status&&<p className="muted">{status}</p>}</section><section className="panel"><h2>Skin & UUID</h2><div className="skinPlaceholder">Live external player data is intentionally disabled until the server-side API and cache layer are configured.</div></section></div><section className="help"><h2>Reliable player data first</h2><p>MineTools validates the input locally but does not fabricate UUIDs, skins or capes. A production lookup needs server-side requests, caching, rate-limit handling and clear error states.</p></section></div></main>}